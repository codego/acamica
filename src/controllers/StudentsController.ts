import * as _ from 'lodash';

class StudentsController {
    static list (req: any, res: any) {
      res.send('Welcome');
    }

    static store (req: any, res: any) {
    }
}

export default StudentsController;
